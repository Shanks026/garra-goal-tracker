# Garra — environment template
# Copy to .env and fill in. NEVER commit .env.
# Anything the client reads must be prefixed EXPO_PUBLIC_ and is therefore
# visible in the shipped bundle — only ever put publishable keys here.

# Supabase (Project Settings → API)
EXPO_PUBLIC_SUPABASE_URL=https://orljqfhudmrvhewlfdkq.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_0LBJwqu9vkSYf2KnkPq7Rg_Hyb9w21I

# Sentry (Phase 1)
EXPO_PUBLIC_SENTRY_DSN=

# RevenueCat (Phase 11)
EXPO_PUBLIC_REVENUECAT_IOS_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=

# ---------------------------------------------------------------------------
# NOT here, deliberately:
#
#   SUPABASE_ACCESS_TOKEN  → .mcp.json only. It is an account-level management
#                            token, has nothing to do with the app runtime, and
#                            must never reach the bundle.
#   service_role key       → never in this repo, in any file, for any reason.
# ---------------------------------------------------------------------------
